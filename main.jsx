import React from "react";
import ReactDOM from "react-dom/client";
import FloorControl from "./FloorControl.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FloorControl />
  </React.StrictMode>
);
